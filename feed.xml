<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://williamykz.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://williamykz.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2025-08-27T18:48:50+00:00</updated><id>https://williamykz.github.io/feed.xml</id><title type="html">blank</title><subtitle>A simple, whitespace theme for academics. Based on [*folio](https://github.com/bogoli/-folio) design. </subtitle><entry><title type="html">Memory Accounting in Machine Learning</title><link href="https://williamykz.github.io/2025/08/10/lecture1.html" rel="alternate" type="text/html" title="Memory Accounting in Machine Learning"/><published>2025-08-10T01:47:00+00:00</published><updated>2025-08-10T01:47:00+00:00</updated><id>https://williamykz.github.io/2025/08/10/lecture1</id><content type="html" xml:base="https://williamykz.github.io/2025/08/10/lecture1.html"><![CDATA[<h2 id="memory-accounting">Memory Accounting</h2> <h3 id="tensor">Tensor</h3> <p>Which is the basic building block for storing everything: parameters, gradients, optimizer state, data, activations.</p> <p>There are multi ways to create tensor for example</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">x</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">tensor</span><span class="p">([</span><span class="mf">1.</span><span class="p">,</span> <span class="mi">2</span><span class="p">,</span> <span class="mi">3</span><span class="p">],</span> <span class="p">[</span><span class="mi">4</span><span class="p">,</span> <span class="mi">5</span><span class="p">,</span> <span class="mi">6</span><span class="p">])</span>
<span class="n">x</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">zeros</span><span class="p">(</span><span class="mi">4</span><span class="p">,</span> <span class="mi">8</span><span class="p">)</span>
</code></pre></div></div> <p>All for those tensore are stored as floating point numbers.</p> <p>In the Machine Learning we typically call float32(FP32) as the full precision, usually is float64 as full precision.</p> <p>Basically the we calculate the memory useage by numel time element size. Here let make an example using FP32</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">x</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">zeros</span><span class="p">(</span><span class="mi">4</span><span class="p">,</span> <span class="mi">8</span><span class="p">)</span>
<span class="k">assert</span> <span class="n">x</span><span class="p">.</span><span class="n">dtype</span> <span class="o">==</span> <span class="n">torch</span><span class="p">.</span><span class="n">float32</span> <span class="c1"># Default type
</span><span class="k">assert</span> <span class="n">x</span><span class="p">.</span><span class="nf">size</span><span class="p">()</span> <span class="o">==</span> <span class="n">torch</span><span class="p">.</span><span class="nc">Size</span><span class="p">([</span><span class="mi">4</span><span class="p">,</span> <span class="mi">8</span><span class="p">])</span>
<span class="k">assert</span> <span class="n">x</span><span class="p">.</span><span class="nf">numel</span><span class="p">()</span> <span class="o">==</span> <span class="mi">4</span> <span class="o">*</span> <span class="mi">8</span>
<span class="k">assert</span> <span class="n">x</span><span class="p">.</span><span class="nf">element_size</span><span class="p">()</span> <span class="o">==</span> <span class="mi">4</span> <span class="c1">#Float is 4 bytes
</span><span class="k">assert</span> <span class="nf">get_memory_useage</span><span class="p">(</span><span class="n">x</span><span class="p">)</span> <span class="o">==</span> <span class="mi">4</span> <span class="o">*</span> <span class="mi">8</span> <span class="o">*</span> <span class="mi">4</span>
</code></pre></div></div> <p>The tensor <code class="language-plaintext highlighter-rouge">x = torch.zeros(4, 8)</code> has 4 * 8 = 32 elements, which is numel, and each elements is of type <code class="language-plaintext highlighter-rouge">float32</code> which is the element size.</p> <h2 id="float-16">Float 16</h2> <p>The float 16 data type cuts down the memory</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">x</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">zeros</span><span class="p">(</span><span class="mi">4</span><span class="p">,</span> <span class="mi">8</span><span class="p">,</span> <span class="n">dtype</span><span class="o">=</span><span class="n">torch</span><span class="p">.</span><span class="n">float16</span><span class="p">)</span>
<span class="k">assert</span> <span class="n">x</span><span class="p">.</span><span class="nf">element_size</span><span class="p">()</span> <span class="o">==</span> <span class="mi">2</span>
</code></pre></div></div> <p>But for the float 16 the dynamic range is not that great. for example if you want to do <code class="language-plaintext highlighter-rouge">x = torch.tensor([1e-8], dtype=torch.float16)</code> which will underflow. So when you train small model <code class="language-plaintext highlighter-rouge">fp16</code> can handel the model, but when you are training the large model <code class="language-plaintext highlighter-rouge">fp16</code> will have instability.</p> <h2 id="bfloat16">bfloat16</h2> <p><code class="language-plaintext highlighter-rouge">bfloat16</code> uses the same memory as <code class="language-plaintext highlighter-rouge">float16</code> but has the same dynamic range as <code class="language-plaintext highlighter-rouge">float32</code> because they have same exponent range which is 8 bit.</p> <p>In deep learning workloads, representing very large or very small number is critical. These values can vary widely across layers and during training. Which mean</p> <ol> <li>dynamic range matters for stability: If the exponent range is too narrow, you risk overflow, or underflow. That can disrupt training serverely.</li> <li>Precision within that range is secondary: small rounding errors in the mantissa tend to be less catatrophic than entirely losing or saturating values.</li> </ol> <h2 id="tensors-location">Tensors location</h2> <p>By default, tensors are stored in CPU memory, but when we are training the deep learning we need to move then tensors to GPU which is</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">if</span> <span class="ow">not</span> <span class="n">torch</span><span class="p">.</span><span class="n">cuda</span><span class="p">.</span><span class="nf">is_available</span><span class="p">():</span>
  <span class="k">return</span>

<span class="n">num_gpus</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="n">cuda</span><span class="p">.</span><span class="nf">device_count</span><span class="p">()</span>
<span class="k">for</span> <span class="n">i</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="n">num_gpus</span><span class="p">):</span>
  <span class="n">properties</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="n">cuda</span><span class="p">.</span><span class="nf">get_device_properties</span><span class="p">(</span><span class="n">i</span><span class="p">)</span>

<span class="n">memory_allocated</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="n">cuda</span><span class="p">.</span><span class="nf">memory_allocated</span><span class="p">()</span>

<span class="c1">#for moving tensor to GPU memory 
</span><span class="n">y</span> <span class="o">=</span> <span class="n">x</span><span class="p">.</span><span class="nf">to</span><span class="p">(</span><span class="sh">"</span><span class="s">cuda:0</span><span class="sh">"</span><span class="p">)</span>

</code></pre></div></div> <h2 id="tensor-storeage">Tensor Storeage</h2> <p>Every <code class="language-plaintext highlighter-rouge">torch.Tensor</code> references a <code class="language-plaintext highlighter-rouge">torch.Storage</code> object: which is one-dimensional, continguous array of butes that holds all the raw tensor data.</p> <p>Here let us make an example for the tensor below</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code> <span class="n">x</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">tensor</span><span class="p">([</span>
        <span class="p">[</span><span class="mf">0.</span><span class="p">,</span> <span class="mi">1</span><span class="p">,</span> <span class="mi">2</span><span class="p">,</span> <span class="mi">3</span><span class="p">],</span>
        <span class="p">[</span><span class="mi">4</span><span class="p">,</span> <span class="mi">5</span><span class="p">,</span> <span class="mi">6</span><span class="p">,</span> <span class="mi">7</span><span class="p">],</span>
        <span class="p">[</span><span class="mi">8</span><span class="p">,</span> <span class="mi">9</span><span class="p">,</span> <span class="mi">10</span><span class="p">,</span> <span class="mi">11</span><span class="p">],</span>
        <span class="p">[</span><span class="mi">12</span><span class="p">,</span> <span class="mi">13</span><span class="p">,</span> <span class="mi">14</span><span class="p">,</span> <span class="mi">15</span><span class="p">],</span>
<span class="p">])</span>
</code></pre></div></div> <p>In the computer the <code class="language-plaintext highlighter-rouge">torch.Storeage</code> will store tensor in a one-dimensional, contiguous array of butes that holds all the raw tensor data, in this example we will store the tensor in to the <code class="language-plaintext highlighter-rouge">[0, 1, 2, 3, 4, ...]</code></p> <p>Now if we want to find an index of an element we need to using the stride to indicates how many elements to skip in the underlying storage to move along each dimension.</p> <ul> <li><code class="language-plaintext highlighter-rouge">stride(0)</code> tells you how many step to take in the storage to move to the next row.</li> <li><code class="language-plaintext highlighter-rouge">stride(1)</code> tells you how many steps to move to go to the next column.</li> </ul> <p>Thus in this example we have a tensor shape in <code class="language-plaintext highlighter-rouge">(4,4)</code> then <code class="language-plaintext highlighter-rouge">x.stride(0) = 4</code> and <code class="language-plaintext highlighter-rouge">x.stride(1) = 1</code>. If we want the element at row = 1 column = 2 then we need to</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">index</span> <span class="o">=</span>  <span class="n">r</span> <span class="o">*</span> <span class="n">x</span><span class="p">.</span><span class="nf">stride</span><span class="p">(</span><span class="mi">0</span><span class="p">)</span> <span class="o">+</span> <span class="n">c</span> <span class="o">*</span> <span class="n">x</span><span class="p">.</span><span class="nf">stride</span><span class="p">(</span><span class="mi">1</span><span class="p">)</span>
</code></pre></div></div> <p>Which is 6</p> <h2 id="tensor-slicing">Tensor Slicing</h2> <p>Many operations simply procide a different view of the tensor, Which mean it does not help you make a copy, and therefore mutations in one tensor affects the other.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># Get row 0
</span><span class="n">y</span> <span class="o">=</span> <span class="n">x</span><span class="p">[</span><span class="mi">0</span><span class="p">]</span>

<span class="c1"># Get column 1
</span><span class="n">y</span> <span class="o">=</span> <span class="n">x</span><span class="p">[:,</span> <span class="mi">1</span><span class="p">]</span>

<span class="c1"># View 2x3 matrix as 3x2 matrix
</span><span class="n">y</span> <span class="o">=</span> <span class="n">x</span><span class="p">.</span><span class="nf">view</span><span class="p">(</span><span class="mi">3</span><span class="p">,</span> <span class="mi">2</span><span class="p">)</span>
</code></pre></div></div> <h2 id="tensor-matmul">Tensor Matmul</h2> <p>For the matrix multiplication</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">x</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">ones</span><span class="p">(</span><span class="mi">16</span><span class="p">,</span> <span class="mi">32</span><span class="p">)</span>
<span class="n">w</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">ones</span><span class="p">(</span><span class="mi">32</span><span class="p">,</span> <span class="mi">2</span><span class="p">)</span>
<span class="n">y</span> <span class="o">=</span> <span class="n">x</span> <span class="o">@</span> <span class="n">w</span>
<span class="k">assert</span> <span class="n">y</span><span class="p">.</span><span class="nf">size</span><span class="p">()</span> <span class="o">==</span> <span class="n">torch</span><span class="p">.</span><span class="nc">Size</span><span class="p">([</span><span class="mi">16</span><span class="p">,</span> <span class="mi">2</span><span class="p">])</span>  
</code></pre></div></div> <p>For the</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">x</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">ones</span><span class="p">(</span><span class="mi">4</span><span class="p">,</span> <span class="mi">8</span><span class="p">,</span> <span class="mi">16</span><span class="p">,</span> <span class="mi">32</span><span class="p">)</span>
<span class="n">w</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">ones</span><span class="p">(</span><span class="mi">32</span><span class="p">,</span> <span class="mi">2</span><span class="p">)</span>
<span class="n">y</span> <span class="o">=</span> <span class="n">x</span> <span class="o">@</span> <span class="n">w</span>
<span class="k">assert</span> <span class="n">y</span><span class="p">.</span><span class="nf">size</span><span class="p">()</span> <span class="o">==</span> <span class="n">torch</span><span class="p">.</span><span class="nc">Size</span><span class="p">([</span><span class="mi">4</span><span class="p">,</span> <span class="mi">8</span><span class="p">,</span> <span class="mi">16</span><span class="p">,</span> <span class="mi">2</span><span class="p">])</span>
</code></pre></div></div> <p>In this case, we iterate over values of the first 2 dimensions of x and multiply by w.</p> <h2 id="tensor-einops">Tensor Einops</h2> <p>It is a way that help you keep track of tensor dimensions. Traditional Pytorch code will be</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">x</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">ones</span><span class="p">(</span><span class="mi">2</span><span class="p">,</span> <span class="mi">2</span><span class="p">,</span> <span class="mi">3</span><span class="p">)</span>
<span class="n">y</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">ones</span><span class="p">(</span><span class="mi">2</span><span class="p">,</span> <span class="mi">2</span><span class="p">,</span> <span class="mi">3</span><span class="p">)</span>
<span class="n">z</span> <span class="o">=</span> <span class="n">x</span> <span class="o">@</span> <span class="n">y</span><span class="p">.</span><span class="nf">transpose</span><span class="p">(</span><span class="o">-</span><span class="mi">2</span><span class="p">,</span> <span class="o">-</span><span class="mi">1</span><span class="p">)</span>
</code></pre></div></div> <p>Here the question is we can not know the what different dimensions means.</p> <p>The jaxtyping will be the <code class="language-plaintext highlighter-rouge">x: Float[torch.Tensor, "batch seq heads hidden"] = torch.ones(2, 2, 1, 3)</code></p> <p>using <code class="language-plaintext highlighter-rouge">einsum</code> help you have matrix multiplication with good bookkeeping.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># define two tensor
</span><span class="n">x</span><span class="p">:</span> <span class="n">Float</span><span class="p">[</span><span class="n">torch</span><span class="p">.</span><span class="n">Tensor</span><span class="p">,</span> <span class="sh">"</span><span class="s">batch seq1 hidden</span><span class="sh">"</span><span class="p">]</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">ones</span><span class="p">(</span><span class="mi">2</span><span class="p">,</span> <span class="mi">3</span><span class="p">,</span> <span class="mi">4</span><span class="p">)</span>
<span class="n">y</span><span class="p">:</span> <span class="n">Float</span><span class="p">[</span><span class="n">torch</span><span class="p">.</span><span class="n">Tensor</span><span class="p">,</span> <span class="sh">"</span><span class="s">batch seq2 hidden</span><span class="sh">"</span><span class="p">]</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">ones</span><span class="p">(</span><span class="mi">2</span><span class="p">,</span> <span class="mi">3</span><span class="p">,</span> <span class="mi">4</span><span class="p">)</span>

<span class="c1"># Old way
</span><span class="n">z</span> <span class="o">=</span> <span class="n">x</span> <span class="o">@</span> <span class="n">y</span><span class="p">.</span><span class="nf">transpose</span><span class="p">(</span><span class="o">-</span><span class="mi">2</span><span class="p">,</span> <span class="mi">1</span><span class="p">)</span>

<span class="n">z</span> <span class="o">=</span> <span class="nf">einsum</span><span class="p">(</span><span class="n">x</span><span class="p">,</span> <span class="n">y</span><span class="p">,</span> <span class="sh">"</span><span class="s">batch seq1 hidden, batch seq2 hidden -&gt; batch seq1 seq2</span><span class="sh">"</span><span class="p">)</span>
</code></pre></div></div> <p>It will first matching the items when it sees <code class="language-plaintext highlighter-rouge">batch</code> is in both inputs and also in the output, so it keeps the batch dimension aligned.</p> <p>Then contraction when it sees the <code class="language-plaintext highlighter-rouge">hidden</code> dimension is present in both inputs but not in the output. It tells <code class="language-plaintext highlighter-rouge">einsum</code> to multiply the elements along the <code class="language-plaintext highlighter-rouge">hidden</code> axis of <code class="language-plaintext highlighter-rouge">x</code> and <code class="language-plaintext highlighter-rouge">y</code> and then sum them up - which is the definition of matrix multiplication.</p> <p>then keeping the same, when it sees <code class="language-plaintext highlighter-rouge">seq1</code> from the first input and <code class="language-plaintext highlighter-rouge">seq2</code> from the second input are in the output, so it keeps those dimensions.</p> <h2 id="tensor-operations-flops">Tensor Operations FLOPS</h2> <p>A floating-point operation(FLOP) is a basic operation like addition (x + y) or multiplication (x y).</p> <ul> <li>FLOPs: floating-point operations (measure of computation done)</li> <li>FLOP/s: floating-point operations per second, which is used to measure the speed of hardware.</li> </ul> <p>give some number here</p> <ul> <li>Training GPT-3 took 3.14 e 23 FLOPs</li> <li>Training GPT-4 took 2e25 FLOPs</li> </ul> <p>A100 has a peak performance of 312 teraFLOP/s</p> <p>H100 has a peak performance of 1979 teraFLOP/s</p> <p>Let make an example using the Linear model</p> <h3 id="linear-model-example">Linear model Example</h3> <p>we have <code class="language-plaintext highlighter-rouge">n</code> points, each point is <code class="language-plaintext highlighter-rouge">d-dimensional</code> and the linear model maps each d-dimensional vector to a k outputs.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">if</span> <span class="n">torch</span><span class="p">.</span><span class="n">cuda</span><span class="p">.</span><span class="nf">is_available</span><span class="p">():</span>
  <span class="n">B</span> <span class="o">=</span> <span class="mi">16384</span>
  <span class="n">D</span> <span class="o">=</span> <span class="mi">32768</span>
  <span class="n">K</span> <span class="o">=</span> <span class="mi">8192</span>

<span class="k">else</span><span class="p">:</span>
  <span class="n">B</span> <span class="o">=</span> <span class="mi">1024</span>
  <span class="n">D</span> <span class="o">=</span> <span class="mi">256</span>
  <span class="n">K</span> <span class="o">=</span> <span class="mi">64</span>

<span class="n">device</span> <span class="o">=</span> <span class="nf">get_device</span><span class="p">()</span>
<span class="n">x</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">ones</span><span class="p">(</span><span class="n">B</span><span class="p">,</span> <span class="n">D</span><span class="p">,</span> <span class="n">device</span> <span class="o">=</span> <span class="n">device</span><span class="p">)</span>
<span class="n">w</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">randn</span><span class="p">(</span><span class="n">D</span><span class="p">,</span> <span class="n">K</span><span class="p">,</span> <span class="n">device</span> <span class="o">=</span> <span class="n">device</span><span class="p">)</span>

<span class="n">y</span> <span class="o">=</span> <span class="n">x</span> <span class="o">@</span> <span class="n">w</span>
<span class="n">actual_num_flops</span> <span class="o">=</span> <span class="mi">2</span> <span class="o">*</span> <span class="n">B</span> <span class="o">*</span> <span class="n">D</span> <span class="o">*</span> <span class="n">K</span>
</code></pre></div></div>]]></content><author><name></name></author><category term="Machine-Learning"/><summary type="html"><![CDATA[Understanding tensor memory usage and calculation in PyTorch]]></summary></entry><entry><title type="html">How to use Claude Code</title><link href="https://williamykz.github.io/2025/08/10/ClaudeCode.html" rel="alternate" type="text/html" title="How to use Claude Code"/><published>2025-08-10T01:47:00+00:00</published><updated>2025-08-10T01:47:00+00:00</updated><id>https://williamykz.github.io/2025/08/10/ClaudeCode</id><content type="html" xml:base="https://williamykz.github.io/2025/08/10/ClaudeCode.html"><![CDATA[<h2 id="download">Download</h2> <p>Software download Node.js 18+ (https://nodejs.org/en/download) git GitHub or GitLab</p> <p>Install Claude Code</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>npm <span class="nb">install</span> <span class="nt">-g</span> @anthropic-ai/claude-code

<span class="nb">cd </span>your-project-directory

claude
</code></pre></div></div> <p>Install github cli <code class="language-plaintext highlighter-rouge">sudo apt install gh</code></p> <h2 id="different-way-to-using-claude">Different way to using claude</h2> <p>Chat box mode(headless mode): <code class="language-plaintext highlighter-rouge">claude -p "tell me a joke"</code> in your terminal</p> <p>Better install jq, <code class="language-plaintext highlighter-rouge">sudo apt install jq</code></p> <p>yolo mode: ignore premissions issues. <code class="language-plaintext highlighter-rouge">claude yolo - p</code></p> <h2 id="claude-code-command">Claude Code Command</h2> <ul> <li><code class="language-plaintext highlighter-rouge">/init</code>: It will help you generate a claude.md file that alow you write instruction for the project and code.</li> <li><code class="language-plaintext highlighter-rouge">@ + file name</code>: let claude code to read that file.</li> <li><code class="language-plaintext highlighter-rouge">ESC</code>: ESC twice will show all the history talk.</li> <li><code class="language-plaintext highlighter-rouge">Install Github App</code></li> <li><code class="language-plaintext highlighter-rouge">/memory</code> and <code class="language-plaintext highlighter-rouge"># [Add memory]</code> for adding memory for your local memory or project memory.</li> </ul> <h2 id="make-you-own-command">Make you own command</h2> <p>Make a commands file <code class="language-plaintext highlighter-rouge">mkdir -p ~/.claude/commands</code></p> <p>Make your own command and you can type and use in claude code <code class="language-plaintext highlighter-rouge">echo "find stock price of $ARGUMENTS right now" &gt; ~/.claude/commands/stockprice.md</code></p> <h2 id="claude-code-mcp">Claude Code MCP</h2> <h3 id="context-7">Context 7</h3> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>claude mcp add <span class="nt">--transport</span> http context7 https://mcp.context7.com/mcp <span class="nt">--header</span> <span class="s2">"CONTEXT7_API_KEY: YOUR_API_KEY"</span>
</code></pre></div></div> <h3 id="modify-mcp-setting-bu-youself">Modify MCP setting bu youself</h3> <p><code class="language-plaintext highlighter-rouge">nano .mcp.json</code></p> <h2 id="hook">Hook</h2> <h2 id="tips">Tips</h2> <ul> <li>Using <code class="language-plaintext highlighter-rouge">Control + R</code> will help you expend section and show all the context.</li> </ul>]]></content><author><name></name></author><category term="Machine-Learning"/><summary type="html"><![CDATA[Download Software download Node.js 18+ (https://nodejs.org/en/download) git GitHub or GitLab]]></summary></entry><entry><title type="html">a post with plotly.js</title><link href="https://williamykz.github.io/sample-posts/2025/03/26/plotly.html" rel="alternate" type="text/html" title="a post with plotly.js"/><published>2025-03-26T14:24:00+00:00</published><updated>2025-03-26T14:24:00+00:00</updated><id>https://williamykz.github.io/sample-posts/2025/03/26/plotly</id><content type="html" xml:base="https://williamykz.github.io/sample-posts/2025/03/26/plotly.html"><![CDATA[<p>This is an example post with some <a href="https://plotly.com/javascript/">plotly</a> code.</p> <div class="language-markdown highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">```</span><span class="nl">plotly
</span><span class="sb">{
  "data": [
    {
      "x": [1, 2, 3, 4],
      "y": [10, 15, 13, 17],
      "type": "scatter"
    },
    {
      "x": [1, 2, 3, 4],
      "y": [16, 5, 11, 9],
      "type": "scatter"
    }
  ]
}</span>
<span class="p">```</span>
</code></pre></div></div> <p>Which generates:</p> <pre><code class="language-plotly">{
  "data": [
    {
      "x": [1, 2, 3, 4],
      "y": [10, 15, 13, 17],
      "type": "scatter"
    },
    {
      "x": [1, 2, 3, 4],
      "y": [16, 5, 11, 9],
      "type": "scatter"
    }
  ]
}
</code></pre> <p>Also another example chart.</p> <div class="language-markdown highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">```</span><span class="nl">plotly
</span><span class="sb">{
  "data": [
    {
      "x": [1, 2, 3, 4],
      "y": [10, 15, 13, 17],
      "mode": "markers"
    },
    {
      "x": [2, 3, 4, 5],
      "y": [16, 5, 11, 9],
      "mode": "lines"
    },
    {
      "x": [1, 2, 3, 4],
      "y": [12, 9, 15, 12],
      "mode": "lines+markers"
    }
  ],
  "layout": {
    "title": {
      "text": "Line and Scatter Plot"
    }
  }
}</span>
<span class="p">```</span>
</code></pre></div></div> <p>This is how it looks like:</p> <pre><code class="language-plotly">{
  "data": [
    {
      "x": [1, 2, 3, 4],
      "y": [10, 15, 13, 17],
      "mode": "markers"
    },
    {
      "x": [2, 3, 4, 5],
      "y": [16, 5, 11, 9],
      "mode": "lines"
    },
    {
      "x": [1, 2, 3, 4],
      "y": [12, 9, 15, 12],
      "mode": "lines+markers"
    }
  ],
  "layout": {
    "title": {
      "text": "Line and Scatter Plot"
    }
  }
}
</code></pre>]]></content><author><name></name></author><category term="sample-posts"/><category term="formatting"/><category term="charts"/><summary type="html"><![CDATA[this is what included plotly.js code could look like]]></summary></entry></feed>